{
  "name": "rollup-plugin-dts-issue",
  "version": "1.0.0",
  "description": "To show an issue with output options",
  "main": "src/index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && rollup --config rollup.config.js",
    "build-dts": "rollup --config rollup.dts.config.js && rm -rf dist/types"
  },
  "dependencies": {
    "@rollup/plugin-typescript": "^8.3.1",
    "@types/node": "^17.0.23",
    "rollup": "^2.70.1",
    "rollup-plugin-dts": "^4.2.0",
    "tslib": "^2.3.1",
    "typescript": "^4.6.3"
  }
}
